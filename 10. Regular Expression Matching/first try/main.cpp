#include <bits/stdc++.h>

using namespace std;


class Solution{
    public:
    bool isMatch(string s, string p){
        for(auto its = s.begin(), itp = p.begin(); its != s.end(); ++its){
            
        }
    }
};

int main(){

    Solution solution;
    string s, p;
    getline(cin, s);
    getline(cin, p);
    solution.isMatch(s, p);

    return 0;
}